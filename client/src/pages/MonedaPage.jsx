
import { MonedaList } from "../components/MonedaList"


/**
 * This is a React component that renders a list of currencies.
 * @returns The `MonedaPage` component is returning the `MonedaList` component.
 */
const MonedaPage = () => {
    return (
        <MonedaList />
    )
}

export default MonedaPage